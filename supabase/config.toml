# Configuration Supabase — générée par Constructor
# Ce fichier est informatif pour l'export / self-hosting.
# En mode Constructor, la base de données est gérée automatiquement.

[project]
id = "my-app"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[auth]
enabled = true
site_url = "http://localhost:5173"
additional_redirect_urls = ["http://localhost:5173"]

[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = false
